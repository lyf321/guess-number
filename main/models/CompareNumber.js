class CompareNumber {

    constructor(randomNumber, guess) {
        this.randomNumber = randomNumber;
        this.guess = guess;
    }

    static guessNumber(randomNumber, guess) {const result = []
        if (randomNumber === guess) {
            return '4A0B';
        }
    }

}
    module.exports = CompareNumber;